.second-container
  .second-container__top
    %h1.logo
      = image_tag 'furima-logo.svg'
    %nav.progress-barsignup
      %ol.clearfix
        %li.through
          会員情報
          .progres-status.through
            %pseudo:after &gt;
            %pseudo:after &gt;
        %li.through
          氏名、生年月日
          .progres-status.active
            %pseudo:after &gt;
            %pseudo:after &gt;
        %li.active
          送付先情報
          .progres-status.point
            %pseudo:after &gt;
            %pseudo:after &gt;
        %li.next
          完了
          .progres-status
  .second-container__main
    .second-container__main__section
      %h2 送付先情報
      = form_for @user, url: sign_up_index_path,method: :post do |f|
        .form_contents
          .form-group
            %h3.text-left 本人確認
            %p.l-single-text
              安心・安全にご利用いただくために、お客さまの本人情報の登録にご協力ください。他のお客さまに公開されることはありません。
          .form-group
            %div
              %label 送付先お名前(全角)
              %span.form-require 必須
            = f.text_field :address_last_name, placeholder: '例) 山田',class:"form-half"
            = f.text_field :address_first_name, placeholder: '例) 彩' ,class:"form-half"
          .form-group
            %div
              %label{:for => "family_name_kanji"} 送付先お名前カナ(全角)
              %span.form-require 必須
            = f.text_field :address_last_name_kana, placeholder: '例) ヤマダ',class:"form-half"
            = f.text_field :address_first_name_kana, placeholder: '例) アヤ',class:"form-half"
          .form-group
            %div
              %label 郵便番号
              %span.form-require 必須
            = f.text_field :postal_code, placeholder: '郵便番号',class:"address_form"
          .form-group
            %div
              %label 都道府県
              %span.form-require 必須
            = f.text_field :prefectures, placeholder: '都道府県',class:"address_form"
          .form-group
            %div
              %label 市区町村
              %span.form-require 必須
            = f.text_field :city, placeholder: "市区町村",class:"address_form"
          .form-group
            %div
              %label 番地
              %span.form-require 必須
            = f.text_field :house_number, placeholder: "番地",class:"address_form"
          .form-group
            %div
              %label マンション、アパート名
              -# %span.form-require 必須
            = f.text_field :building_name, placeholder: "マンション、アパート名",class:"address_form"

            .clearfix
          %p.form-info-text
            ※ 本人情報は正しく入力してください。会員登録後、修正するにはお時間を頂く場合があります。
            = f.submit '登録完了',class: "next-submit"
            %p ※電話番号は本人確認や不正利用防止のために利用します。他のユーザーに公開されることはありません。
